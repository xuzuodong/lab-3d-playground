import Vue from 'vue'
import VueSnip from 'vue-snip'

Vue.use(VueSnip)

/**
 * https://www.npmjs.com/package/vue-snip
 * Vue.js directive that clamps the content of a text element 
 * if it exceeds the specified number of lines. 
 */
